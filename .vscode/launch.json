{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Rendertron",
      "skipFiles": ["<node_internals>/**"],
      "program": "${workspaceFolder}/build/rendertron.js",
      "preLaunchTask": "build"
    }
  ]
}
